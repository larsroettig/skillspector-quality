# Extra Notes

More notes about the processor and processing data. The processor processes data. Data
gets processed. Processing happens. The processed data is data that has been processed.

This file is not linked from SKILL.md, so it is an orphan in the link graph.
